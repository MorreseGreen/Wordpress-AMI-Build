---
- hosts: default
  become: true
  tasks:
    - name: Give time for servers to boot up and install all files
      script: script1.sh
    
    - name: Update Configuration File
      script: script2.sh
    